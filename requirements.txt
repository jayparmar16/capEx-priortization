# Minimal dependencies for the Streamlit Community Cloud deployment.
# The dashboard (src/dashboard.py) and reranker (src/reranker.py) only use these.
# The full pipeline dependencies (LangGraph, scikit-learn, jupyter, etc.) live in
# requirements-dev.txt for local development and re-running the data pipeline.
streamlit
pandas
numpy
matplotlib
seaborn
folium
streamlit-folium
